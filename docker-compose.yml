version: '3.8'

services:
  ppt-translator:
    build: .
    container_name: ppt-translator
    ports:
      - "5111:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./outputs:/app/outputs
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
